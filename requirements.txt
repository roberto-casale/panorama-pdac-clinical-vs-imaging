# PANORAMA PDAC — dipendenze Python per il notebook di download
# Richiede Python ESATTAMENTE 3.12.13 (fissato in .python-version; usa pyenv install 3.12.13).
# Ricrea l'ambiente con:
#   pyenv local 3.12.13 && python -m venv .venv && .venv/bin/pip install -r requirements.txt
#
# (Curato e cross-platform: solo cio' che serve a panorama_download.ipynb.
#  Eventuali tool DICOM per altri dataset, es. pydicom/dcm2niix, non sono inclusi.)
remotezip==0.12.3
requests==2.34.2
openpyxl==3.1.5
pandas==3.0.3
numpy==2.4.6
nibabel==5.4.2
ipykernel==7.2.0
jupyterlab==4.5.7
nbconvert==7.17.1
nbformat==5.10.4
